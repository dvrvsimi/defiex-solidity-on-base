import { DeploymentParameters } from "../../../../types/deploy";
import { Future } from "../../../../types/module";
import { DeploymentLoader } from "../../../deployment-loader/types";
import { DeploymentState } from "../../types/deployment-state";
import { JournalMessage } from "../../types/messages";
export declare function buildInitializeMessageFor(future: Future, deploymentState: DeploymentState, strategy: {
    name: string;
}, deploymentParameters: DeploymentParameters, deploymentLoader: DeploymentLoader, accounts: string[], defaultSender: string): Promise<JournalMessage>;
//# sourceMappingURL=build-initialize-message-for.d.ts.map